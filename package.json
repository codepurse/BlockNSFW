{
  "name": "blocknsfw",
  "version": "1.6.0",
  "private": true,
  "description": "BlockNSFW browser extension source and tooling.",
  "scripts": {
    "test": "node --test tests/",
    "test:watch": "node --test --watch tests/",
    "lint:firefox": "web-ext lint --source-dir dist/firefox --output text"
  },
  "devDependencies": {
    "web-ext": "^8.3.0"
  }
}
